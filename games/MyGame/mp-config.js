/**
 * mp-config.js — Multiplayer server URL config.
 *
 * LOCAL DEV: leave MP_SERVER_HOST as null (connects to same host automatically).
 *
 * PRODUCTION (after deploying server.mjs to Railway/Render):
 *   Set MP_SERVER_HOST to your server's domain, e.g.:
 *   window.MP_SERVER_HOST = 'your-app.up.railway.app';
 */
window.MP_SERVER_HOST = 'dungeon-crawler-production-af48.up.railway.app';
